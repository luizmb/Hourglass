name: Release

on:
  push:
    branches:
      - 'release/*'
    tags:
      - 'v*'

permissions:
  contents: write

jobs:
  # ============================================================================
  # RELEASE CANDIDATE BUILD - Triggered on release/* branches
  # ============================================================================

  rc-test:
    name: RC - Test & Validate
    if: startsWith(github.ref, 'refs/heads/release/')
    runs-on: macos-26
    env:
      HOMEBREW_NO_AUTO_UPDATE: "1"
      HOMEBREW_NO_INSTALL_CLEANUP: "1"
    steps:
      - uses: actions/checkout@v7

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_26.5.app

      - name: Cache Mint packages
        uses: actions/cache@v6
        with:
          path: ~/.mint
          key: ${{ runner.os }}-mint-${{ hashFiles('Mintfile') }}
          restore-keys: ${{ runner.os }}-mint-

      - name: Cache Homebrew downloads
        uses: actions/cache@v6
        with:
          path: ~/Library/Caches/Homebrew
          key: ${{ runner.os }}-homebrew-${{ hashFiles('.github/workflows/release.yml') }}
          restore-keys: ${{ runner.os }}-homebrew-

      - name: Install Mint
        run: brew install mint

      - name: Install SwiftLint and Periphery
        run: mint bootstrap

      - name: Lint
        run: mint run swiftlint lint --strict

      - name: Build
        run: swift build --build-tests

      - name: Test
        run: swift test --no-parallel

      - name: Periphery
        run: mint run periphery scan

  rc-test-linux:
    name: RC - Test (Linux)
    if: startsWith(github.ref, 'refs/heads/release/')
    runs-on: ubuntu-latest
    needs: rc-test
    steps:
      - uses: actions/checkout@v7

      - name: Build
        run: swift build 2>&1

      - name: Test
        run: swift test --no-parallel 2>&1

  rc-test-android:
    name: RC - Test (Android)
    if: startsWith(github.ref, 'refs/heads/release/')
    runs-on: ubuntu-latest
    needs: rc-test
    steps:
      - uses: actions/checkout@v7

      - name: Test Swift Package on Android
        uses: skiptools/swift-android-action@v2
        with:
          free-disk-space: true

  rc-test-windows:
    name: RC - Test (Windows)
    if: startsWith(github.ref, 'refs/heads/release/')
    runs-on: windows-latest
    needs: rc-test
    steps:
      - uses: compnerd/gha-setup-swift@main
        with:
          swift-version: swift-6.3-release
          swift-build: 6.3-RELEASE

      - uses: actions/checkout@v7

      - name: Build
        run: swift build

      - name: Test
        run: swift test

  rc-build-xcframework:
    name: RC - Build XCFramework
    if: startsWith(github.ref, 'refs/heads/release/')
    runs-on: macos-26
    needs: rc-test
    strategy:
      matrix:
        framework: [Hourglass]
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode_26.5.app

      - name: Resolve Package Dependencies
        run: swift package resolve

      - name: Build Library in Release Configuration
        run: |
          set -x
          swift build -c release --product ${{ matrix.framework }} -v
          echo "✓ Build succeeded"

      - name: Create XCFramework Structure
        run: |
          XCFWK_PATH="build/${{ matrix.framework }}/${{ matrix.framework }}.xcframework"
          mkdir -p "$XCFWK_PATH"

          # Copy Swift module files
          for ext in swiftmodule swiftdoc abi.json swiftsourceinfo; do
            if [ -f ".build/release/Modules/${{ matrix.framework }}.$ext" ]; then
              cp ".build/release/Modules/${{ matrix.framework }}.$ext" "$XCFWK_PATH/"
              echo "✓ Copied ${{ matrix.framework }}.$ext"
            fi
          done

          echo "✓ XCFramework structure created"

      - name: Verify XCFramework Created
        run: |
          XCFWK_PATH="build/${{ matrix.framework }}/${{ matrix.framework }}.xcframework"

          if [ -d "$XCFWK_PATH" ]; then
            echo "✓ XCFramework structure created"
            ls -lah "$XCFWK_PATH"
            find "$XCFWK_PATH" -type f
          else
            echo "✗ XCFramework not found"
            find build -type d -o -type f | head -50
            exit 1
          fi

      - name: Archive XCFramework
        run: |
          cd build/${{ matrix.framework }}
          zip -r ${{ matrix.framework }}.xcframework.zip ${{ matrix.framework }}.xcframework
          cd ../..
          ls -lh build/${{ matrix.framework }}/${{ matrix.framework }}.xcframework.zip

      - name: Upload XCFramework Artifact
        uses: actions/upload-artifact@v7
        with:
          name: ${{ matrix.framework }}-xcframework
          path: build/${{ matrix.framework }}/${{ matrix.framework }}.xcframework.zip
          retention-days: 90

  rc-notify-status:
    name: RC - Notify Status
    if: startsWith(github.ref, 'refs/heads/release/')
    runs-on: ubuntu-latest
    needs: [rc-test, rc-test-linux, rc-test-android, rc-test-windows, rc-build-xcframework]
    steps:
      - name: Extract Version
        id: version
        run: |
          BRANCH="${{ github.ref_name }}"
          VERSION=${BRANCH#release/}
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Success Notification
        if: success()
        run: |
          echo "::notice::Release Candidate ${{ steps.version.outputs.version }} is ready for promotion"
          echo "Run 'Promote RC to Release' workflow when ready."

      - name: Failure Notification
        if: failure()
        run: |
          echo "::error::Release Candidate build failed — review failed jobs and push fixes to ${{ github.ref_name }}"

  # ============================================================================
  # RELEASE PROMOTION - Triggered on version tags (v*.*.*)
  # ============================================================================

  promote-test:
    name: Promote - Verify Tag
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Validate Tag Format
        run: |
          TAG="${{ github.ref_name }}"
          if [[ ! "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+ ]]; then
            echo "::error::Invalid tag format: $TAG — expected vX.Y.Z"
            exit 1
          fi
          echo "::notice::Promoting tag: $TAG"

      - name: Verify Tag on Release Branch
        run: |
          TAG="${{ github.ref_name }}"
          VERSION=${TAG#v}
          if git show-ref --verify --quiet "refs/remotes/origin/release/$VERSION"; then
            echo "::notice::Found corresponding release branch: release/$VERSION"
          else
            echo "::warning::No release branch found for release/$VERSION — proceeding if tag is on main"
          fi

  create-release:
    name: Promote - Create GitHub Release
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    needs: promote-test
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Download RC Artifacts
        run: |
          TAG="${{ github.ref_name }}"
          VERSION=${TAG#v}
          BRANCH="release/$VERSION"

          RUN_ID=$(gh api "repos/${{ github.repository }}/actions/runs?branch=${BRANCH}&status=success&per_page=20" \
            --jq '[.workflow_runs[] | select(.name == "Release")] | first | .id')

          if [ -z "$RUN_ID" ] || [ "$RUN_ID" = "null" ]; then
            echo "::error::No successful RC build found for branch $BRANCH"
            exit 1
          fi

          echo "::notice::Downloading artifacts from run $RUN_ID (branch: $BRANCH)"
          mkdir -p artifacts
          gh run download "$RUN_ID" --dir artifacts
        env:
          GH_TOKEN: ${{ github.token }}

      - name: Generate Release Notes
        id: notes
        run: |
          TAG="${{ github.ref_name }}"
          PREV_TAG=$(git describe --tags --abbrev=0 ${TAG}^ 2>/dev/null || echo "")
          if [ -z "$PREV_TAG" ]; then
            BODY="Initial release"
          else
            COMMITS=$(git log ${PREV_TAG}..${TAG} --oneline)
            BODY="## Changes

          ${COMMITS}

          ## Installation

          \`\`\`swift
          .package(url: \"https://github.com/luizmb/Hourglass\", from: \"${TAG#v}\")
          \`\`\`"
          fi
          echo "body<<EOF" >> $GITHUB_OUTPUT
          echo "$BODY" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: List Artifacts
        run: |
          echo "Found artifacts:"
          find artifacts -name "*.zip" -exec ls -lh {} \;

      - name: Create Release
        run: |
          gh release create "${{ github.ref_name }}" \
            --title "Release ${{ github.ref_name }}" \
            --notes "${{ steps.notes.outputs.body }}" \
            artifacts/*/*.zip
        env:
          GH_TOKEN: ${{ github.token }}

  notify-promotion-status:
    name: Promote - Notify Status
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    needs: [promote-test, create-release]
    steps:
      - name: Success Notification
        if: success()
        run: echo "::notice::Release ${{ github.ref_name }} published — ${{ github.server_url }}/${{ github.repository }}/releases/tag/${{ github.ref_name }}"

      - name: Failure Notification
        if: failure()
        run: |
          echo "::error::Release promotion failed for ${{ github.ref_name }}"
          echo "Recovery: delete tag, fix issues, re-tag."
